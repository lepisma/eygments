#+TITLE: eygments

(Approximately) Export Emacs color themes as [[https://pygments.org][pygments]] compatible CSS.

Switch to your desired theme in Emacs, then run ~eygments-export~ to insert CSS
at current point in buffer.
